/**
 * MDS Components - Atomic Design Structure
 * 
 * Components are organized following Atomic Design methodology:
 * - Atoms: Basic building blocks (Button, Input, Icon, etc.)
 * - Molecules: Simple compositions (SearchBar, FormField, etc.)
 * - Organisms: Complex sections (Header, Card, Modal, etc.)
 * - Templates: Page layouts (rarely in DS, more in apps)
 */

// Export all atoms
export * from './atoms';

// Export all molecules
export * from './molecules';

// Export all organisms
export * from './organisms';

// Export all templates (if any)
export * from './templates';

